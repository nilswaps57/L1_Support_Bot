# The current frontend reads no Vite environment variables.
# Its API client currently uses http://localhost:8000/api/v1 directly.
# Keep provider keys, database settings, Qdrant settings, and storage paths out
# of this file. A future browser-safe API URL requires a code change first.
